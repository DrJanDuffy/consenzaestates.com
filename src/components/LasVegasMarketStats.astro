
---
---
<section class="market-stats-section">
  <h2 class="section-title gold-accent">Las Vegas Market Insights</h2>
  <p class="section-description">Insights into the current Las Vegas luxury real estate market trends and statistics.</p>
  
  <div class="stats-container">
    <div class="stat-card">
      <div class="stat-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M3 3v18h18"></path>
          <path d="M18.4 7.8l-8.5 8.4L6 12"></path>
        </svg>
      </div>
      <div class="stat-number" data-value="14.2">14.2<span>%</span></div>
      <div class="stat-label">Appreciation Rate</div>
      <div class="stat-description">Year-over-year increase in luxury home values</div>
    </div>
    
    <div class="stat-card">
      <div class="stat-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0118 0z"></path>
          <circle cx="12" cy="10" r="3"></circle>
        </svg>
      </div>
      <div class="stat-number" data-value="42">42</div>
      <div class="stat-label">Days on Market</div>
      <div class="stat-description">Average time to sell luxury properties</div>
    </div>
    
    <div class="stat-card">
      <div class="stat-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M12 1v22"></path>
          <path d="M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"></path>
        </svg>
      </div>
      <div class="stat-number" data-value="1.2">1.2<span>M</span></div>
      <div class="stat-label">Average Price</div>
      <div class="stat-description">For luxury homes in Las Vegas</div>
    </div>
    
    <div class="stat-card">
      <div class="stat-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M1 6v16l7-4 8 4 7-4V2l-7 4-8-4-7 4"></path>
          <path d="M8 2v16"></path>
          <path d="M16 6v16"></path>
        </svg>
      </div>
      <div class="stat-number" data-value="96.4">96.4<span>%</span></div>
      <div class="stat-label">List-to-Sale Ratio</div>
      <div class="stat-description">Close to asking price in luxury segment</div>
    </div>
  </div>
  
  <div class="market-insights">
    <div class="insight-card">
      <h3>Luxury Market Trends</h3>
      <p>The Las Vegas luxury real estate market continues to thrive with strong demand from out-of-state buyers, particularly from California. Limited inventory of high-end properties has created favorable conditions for sellers, while buyers benefit from relatively lower prices compared to other major luxury markets.</p>
    </div>
    
    <div class="insight-card">
      <h3>Investment Potential</h3>
      <p>Las Vegas luxury homes offer strong investment potential with steady appreciation rates and rental income opportunities. The city's growing economy, no state income tax, and expanding luxury amenities continue to attract high-net-worth individuals looking for primary or secondary homes.</p>
    </div>
  </div>
</section>

<script>
  // Simple animation for stat numbers
  document.addEventListener('DOMContentLoaded', () => {
    const statNumbers = document.querySelectorAll('.stat-number');
    
    // Only animate when the element is in view
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          animateValue(entry.target);
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.1 });
    
    statNumbers.forEach(statNumber => {
      observer.observe(statNumber);
    });
    
    function animateValue(element) {
      const targetValue = parseFloat(element.getAttribute('data-value'));
      const duration = 2000; // 2 seconds
      const step = targetValue / (duration / 16);
      let currentValue = 0;
      
      const timer = setInterval(() => {
        currentValue += step;
        
        if (currentValue >= targetValue) {
          clearInterval(timer);
          currentValue = targetValue;
        }
        
        // Keep the original HTML structure
        const suffix = element.querySelector('span') ? element.querySelector('span').outerHTML : '';
        element.innerHTML = currentValue.toFixed(1).replace(/\.0$/, '') + (suffix || '');
      }, 16);
    }
  });
</script>

<style>
.market-stats-section {
  padding: 4rem 0;
}

.section-title {
  text-align: center;
  margin-bottom: 0.5rem;
  position: relative;
  display: inline-block;
}

.section-description {
  text-align: center;
  max-width: 800px;
  margin: 0 auto 3rem;
  color: var(--theme-on-bg);
  opacity: 0.8;
}

.stats-container {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 2rem;
  margin-bottom: 3rem;
}

.stat-card {
  position: relative;
  background: white;
  border-radius: 12px;
  padding: 2rem;
  text-align: center;
  box-shadow: var(--luxury-shadow-md, 0 10px 15px rgba(0,0,0,0.1));
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  border-bottom: 4px solid var(--luxury-gold, #D4AF37);
  overflow: hidden;
}

.stat-card::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 5px;
  background: var(--luxury-gradient, linear-gradient(to right, #B8860B, #D4AF37, #F1E8BC));
  opacity: 0;
  transition: opacity 0.3s ease;
}

.stat-card:hover {
  transform: translateY(-10px);
  box-shadow: var(--luxury-shadow-lg, 0 20px 25px rgba(0,0,0,0.15));
}

.stat-card:hover::before {
  opacity: 1;
}

.stat-icon {
  width: 50px;
  height: 50px;
  margin: 0 auto 1rem;
  border-radius: 50%;
  background: var(--theme-surface-1);
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--luxury-gold, #D4AF37);
}

.stat-icon svg {
  width: 24px;
  height: 24px;
}

.stat-number {
  font-size: 2.5rem;
  font-weight: 800;
  margin-bottom: 0.5rem;
  color: var(--luxury-gold, #D4AF37);
}

.stat-label {
  font-weight: 600;
  margin-bottom: 0.5rem;
  font-size: 1.1rem;
}

.stat-description {
  font-size: 0.9rem;
  color: var(--theme-on-bg);
  opacity: 0.7;
}

.market-insights {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
}

.insight-card {
  background: var(--theme-surface-1);
  border-radius: 12px;
  padding: 2rem;
  box-shadow: var(--luxury-shadow-sm, 0 4px 6px rgba(0,0,0,0.05));
}

.insight-card h3 {
  margin-bottom: 1rem;
  position: relative;
  padding-bottom: 0.5rem;
}

.insight-card h3::after {
  content: "";
  position: absolute;
  bottom: 0;
  left: 0;
  width: 40px;
  height: 2px;
  background: var(--luxury-gold, #D4AF37);
}

@media (max-width: 768px) {
  .stats-container {
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  }
}
</style>
