---
import Layout from '../../layouts/Page.astro';
import { Container } from '@components/odyssey-theme';
import CalendlyInline from '../../components/CalendlyInline.astro';
import RealScoutListings from '../../components/RealScoutListings.astro';
import settings from '../../config/settings.js';

const seo = {
	title: 'Contact | Astro Odyssey Theme',
	description: 'Contact Page',
};

const phoneDigits = settings.contact.phone.replace(/\D/g, '').slice(-10);
const phoneDisplay = phoneDigits ? `(${phoneDigits.slice(0,3)}) ${phoneDigits.slice(3,6)}-${phoneDigits.slice(6)}` : settings.contact.phone;
---

<Layout seo={seo}>
	<RealScoutListings sectionTitle="Homes for Sale in Las Vegas" />
	<Container>
		<section class="contact-hero__section">
			<div class="contact-hero__text-section">
				<h1>Contact Us</h1>
				<p>Schedule a time that works for you. Use the calendar to book a call or meeting.</p>
				<h2>Get in Touch</h2>
				<h3>Phone & Email</h3>
				<div>
					<p>
						<em><a href={`tel:+1${phoneDigits}`}>{phoneDisplay}</a></em> • <em><a href={`mailto:${settings.contact.email}`}>{settings.contact.email}</a></em>
					</p>
				</div>
				<h3>Schedule Below</h3>
				<p>Use the calendar to the right to book a free 15‑minute call.</p>
			</div>
			<div class="col-2__container">
				<CalendlyInline minWidth="320px" height="700px" utmCampaign="company_contact_inline" />
			</div>
		</section>
	</Container>
</Layout>

<style>
	.contact-hero__section {
		margin: var(--section-margin) auto;
		display: grid;
		grid-template-columns: 1fr 1fr;
		min-height: calc(90vh - var(--navbar-height));
		gap: 3rem;
	}
	.contact-hero__text-section {
		padding: 2rem;
		display: flex;
		flex-direction: column;
		justify-content: center;
		background-color: var(--theme-surface-1);
		border-radius: var(--theme-shape-radius);
	}
	.col-2__container {
		display: flex;
		flex-direction: column;
		justify-content: center;
	}
	@media (max-width: 768px) {
		.contact-hero__section {
			grid-template-columns: 1fr;
		}
	}
</style>
